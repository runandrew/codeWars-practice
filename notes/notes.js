
// Funcitons to know
array.sort(); // Sorts an array
array.split(); // The split() method is used to split a string into an array of substrings, and returns the new array.
array.join(); // Join the elements of an array into a string
array.push(); // Adds an element(s) to the end of an array
array.filter(function(){}); // The filter() method creates a new array with all elements that pass the test implemented by the provided function.
array.reverse(); // The reverse() method reverses an array in place. The first array element becomes the last and the last becomes the first.

string.charCodeAt(indexOfChar); // Return the Unicode of the first character in a string
string.indexOf(); // Finds a match of the string in the argument and returns the position
string.replace(); // The replace() method searches a string for a specified value, or a regular expression, and returns a new string where the specified values are replaced.
string.fromCharCode(); // Reutrns the letter corresponding to the Unicode number given
string.charAt(); // The charAt() method returns the specified character from a string.
string.slice(); // The slice() method extracts a section of a string and returns a new string.

Boolean(); // A function that determines if a value is true
parseInt(); // Converts a string into a number

Math.sqrt(); // Square root
Math.floor(); // Round down the number
